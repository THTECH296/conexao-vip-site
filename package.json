{
  "name": "conexao-vip-site",
  "version": "1.0.0",
  "description": "Landing page institucional da Conexão VIP — provedor de internet fibra óptica.",
  "private": true,
  "scripts": {
    "build:css": "sass scss/main.scss css/style.css --style=compressed --no-source-map",
    "build:js": "tsc",
    "build": "npm run build:css && npm run build:js",
    "watch:css": "sass --watch scss/main.scss:css/style.css",
    "watch:js": "tsc --watch"
  },
  "devDependencies": {
    "sass": "^1.77.0",
    "typescript": "^5.4.0"
  }
}
